\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{natbib}
\usepackage{microtype}
\usepackage{xcolor}

\hypersetup{
    colorlinks=true,
    linkcolor=blue!60!black,
    citecolor=blue!60!black,
    urlcolor=blue!60!black,
}

\title{Volcan: Morphogenetic Cellular Automata for Abstract Reasoning via Code-Dreamed Pretraining}

\author{%
    Tom Park\thanks{Independent researcher. Correspondence: \texttt{tompark0927@gmail.com}.}%
}

\date{\today}

\begin{document}

\maketitle

\begin{abstract}
We present \textbf{Volcan}, a 111{,}195-parameter Neural Cellular Automaton (NCA) for the ARC-AGI-2 abstract reasoning benchmark. Volcan is built on five biologically motivated architectural pillars --- anisotropic directional forces, sparse long-range mycelial attention, Laplacian spectral tension, bioelectric ghost-field pre-pattern, and apoptotic pruning --- trained via masked denoising with in-context learning on synthetic tasks generated by LLM-written Python transformation functions. On a 30-task held-out subset of ARC-AGI-2's public training set, Volcan achieves \textbf{9.7\% exact-match accuracy} (3/31 test inputs) using pretraining combined with $D_8$-augmented LoRA-rank-16 test-time training. This places Volcan above TRM (8\% at 7M parameters) at \textbf{1.6\% of TRM's parameter count}. Through seven controlled experiments varying architecture, data scale, model capacity, rule diversity, TTT capacity, hierarchical structure, and meta-learned LoRA initialization, we demonstrate that the 9.7\% outcome is a stable ceiling for this class of approach, not a tuning artifact. We publish negative results showing that additional capacity, data, adapter expressiveness, multi-timescale hierarchy, and a trained HyperNetwork providing a warm-start LoRA init do not unlock further held-out tasks; the Hyper-TTT variant in particular \emph{regressed} to 2/31, indicating that a weakly-trained meta-init is worse than standard Kaiming zero-mean init. To our knowledge this is the first NCA with a published ARC-AGI-2 pretraining-plus-TTT result; prior NCAs (CompressARC at 76K params, ARC-NCA at $\sim$100K params) have only ARC-AGI-1 results or operate without pretraining.
\end{abstract}

\section{Introduction}
\label{sec:intro}

The ARC-AGI-2 benchmark \citep{chollet2025arcagi2} measures abstract reasoning from few-shot examples on 2D color grid puzzles. Current Kaggle-envelope state of the art is NVARC \citep{sorokin2025nvarc} (4B params, 24\%); the smallest competitive system is TRM \citep{jolicoeur2025trm} (7M params, 8\%). Pure NCA approaches \citep{guichard2025arcnca,liao2025compressarc} have reached up to 17.6\% on ARC-AGI-1 but have published no ARC-AGI-2 numbers.

The open question we address: \textbf{can a sub-million-parameter NCA produce a useful ARC-AGI-2 result through biologically motivated priors and careful pretraining?} Our answer is a qualified yes, with 9.7\% on a 30-task sample at 111K parameters --- a new efficiency point --- but we also show this is a real ceiling rather than a starting point for a capacity-scaling story.

\paragraph{Contributions.}
\begin{enumerate}
    \item A five-pillar biologically-inspired NCA architecture (Volcan) with explicit computational forms for directional tensegrity, mycelial long-range attention, bioelectric ghost pre-pattern, masked denoising dynamics, and apoptotic pruning.
    \item A code-dreamed synthetic task pipeline in which an LLM writes Python transformation functions that procedurally generate consistent demonstration grids, avoiding the consistency failures of LLM-written JSON demos.
    \item Evidence that $D_8$-augmented LoRA-rank-16 test-time training is the key inference-side unlock for small-model ARC-AGI-2 performance: without it, pretraining alone scores 0/31.
    \item \textbf{A systematic ablation showing that the 9.7\% ceiling is robust} to architecture changes (5-pillar vs.\ variants), data scale (43 $\rightarrow$ 325 tasks), capacity (111K $\rightarrow$ 424K via mixture-of-experts), rule-seed diversity (20 $\rightarrow$ 70 families), adapter rank (LoRA 16 $\rightarrow$ 32), hierarchical macro-cells (HRM-style multi-timescale), and meta-learned LoRA initialization (Hyper-TTT). We interpret this as evidence that the bottleneck for small-model ARC-AGI-2 is neither capacity nor data nor init alone, but the distributional gap between any plausible hand-engineered synthetic corpus and real ARC-AGI-2's compositional structure.
\end{enumerate}

\section{Related Work}

\paragraph{NCA for reasoning.} \citet{mordvintsev2020growing} introduced Growing NCAs; \citet{guichard2025arcnca} published ARC-NCA at 17.6\% ensemble on ARC-AGI-1 subset with $\sim$100K parameters and from-scratch per-task training; \citet{liao2025compressarc} presented CompressARC at 4\% on ARC-AGI-2 with 76K parameters and MDL-only training. None use pretraining plus test-time fine-tuning as we do.

\paragraph{Test-time training on ARC.} MIT TTT \citep{akyurek2024ttt} established the canonical recipe (LoRA per task, $D_8$ augmentation, leave-one-out) on transformer backbones, reaching 53\% on ARC-AGI-1 public validation (61.9\% when ensembled with program synthesis). ARChitects \citep{architects2025} refined this with product-of-experts scoring. We port the key pieces of this recipe ($D_8$ augmentation, LoRA rank 16) to the NCA substrate.

\paragraph{Small recursive models.} TRM \citep{jolicoeur2025trm} at 7M parameters with recursive latent refinement reaches 8\% on ARC-AGI-2 and 45\% on ARC-AGI-1 --- the strongest small-model baseline. Our result sits above TRM on ARC-AGI-2 at substantially smaller parameter count, though on a smaller held-out sample.

\paragraph{LLM-generated synthetic data.} NVARC \citep{sorokin2025nvarc} used gpt-oss-120B to generate 103K synthetic tasks, achieving 24\% on ARC-AGI-2. We use qwen2.5:7b to generate Python functions (not demos), which we execute to produce guaranteed-consistent demos.

\paragraph{Bioelectric morphogenesis.} Our ghost-field pre-pattern draws inspiration from Levin's work on bioelectric morphogenesis \citep{levin2014molecular,levin2021bioelectric}, in which embryonic tissues establish voltage-gradient pre-patterns before cellular differentiation.

\paragraph{Parameter-efficient adaptation.} LoRA \citep{hu2022lora} provides low-rank additive updates to a frozen base; we use it as the per-task TTT adapter.

\section{Architecture (Volcan)}
\label{sec:architecture}

Volcan is a five-pillar NCA with 111{,}195 trainable parameters in its base dense configuration. Each pillar has biological motivation and a specific computational form.

\subsection{Multi-scale force transmission (Pillar 1)}

Three parallel mechanisms:
\begin{itemize}
    \item \textbf{Anisotropic directional forces.} Each cell outputs 8 distinct message vectors (one per Moore-neighbor direction, 4 dims each, 32 total). This breaks the isotropy of standard NCAs and enables learned ``push''-like dynamics.
    \item \textbf{Mycelial sparse long-range attention.} Each cell has 4 hyphal partners selected via symmetric small-world topology: three deterministic mirror partners (horizontal flip, vertical flip, 180\textdegree{} rotation) and one random distant cell. The cell attends to these partners with learned weights. This injects a hard symmetry prior while preserving one channel for task-specific long-range discovery.
    \item \textbf{Laplacian spectral tension.} The 16 lowest eigenvectors of the grid graph Laplacian are precomputed. The cell state is projected onto this basis to produce a per-step ``tension'' vector broadcast to every cell. This gives $O(1)$-iteration global information flow.
\end{itemize}

\subsection{Resonance detection (Pillar 2)}

Echo metrics $\mathrm{echo}_k = \cos(s_t, s_{t-k})$ for $k \in \{1,\ldots,4\}$ classify the dynamical regime: $\mathrm{echo}_1 \approx 1$ indicates a stable fixed point; $\mathrm{echo}_2 \approx 1$ with $\mathrm{echo}_1$ low indicates period-2 oscillation between competing rules. This provides a training signal (rewarding stable convergence) and an inference-time confidence measure used in $D_8$-ensemble candidate ranking.

\subsection{Sequential bioelectric to pixel (Pillar 3)}

Execution runs in two phases:
\begin{itemize}
    \item \textbf{Phase A (Ghost Dream).} Color channels are clamped to the input; ghost/hidden/force channels evolve. The NCA ``thinks'' about the input without committing to an output. Phase A terminates when ghost-field $\mathrm{echo}_1 > 0.97$ or after a step cap.
    \item \textbf{Phase B (Crystallization).} Ghost channels freeze (or slow); color channels unclamp and denoise toward the target. This mirrors the biological pattern in which bioelectric pre-patterns \citep{levin2014molecular} stabilize before tissue differentiation.
\end{itemize}

\subsection{Masked denoising training (Pillar 4)}

During training, demo target outputs are corrupted with Bernoulli-masked uniform noise at a sampled rate $\sigma \in [0.2, 0.95]$. Phase B is initialized with this corrupted target; the model is trained to denoise it toward the clean target. This replaces cross-entropy and draws from the masked-diffusion LM literature \citep{architects2025}.

\subsection{Apoptotic pruning (Pillar 5)}

A soft coherence-weighted penalty encourages cells that fail to participate in the emerging dynamical regime to commit toward background. We include this in the codebase but measure its contribution as marginal; it is kept for biological completeness.

\subsection{Additional inductive biases}

\begin{itemize}
    \item \textbf{Objectness.} Connected-component segmentation of the input produces a per-cell object embedding (4-dim) that the NCA reads as static context.
    \item \textbf{``Outside'' token.} An 11th color token represents padding, enabling variable-size tasks in a fixed $30 \times 30$ canvas.
\end{itemize}

\section{Data Pipeline: Code-Dreamed Synthetic Tasks}

\subsection{Failure of JSON-dream pipelines}

Our first synthetic data attempt prompted qwen2.5:7b to output ARC-shaped JSON directly. Visual inspection revealed that the LLM frequently hallucinated demos inconsistent with the stated rule (e.g., demo 0 used color $2 \to 3$ while demo 1 used $2 \to 4$), while our overfit filter still accepted them because 3-demo memorization succeeds even on inconsistent sets. Yield was 71\% but quality was unreliable.

\subsection{Code-dream pipeline}

We inverted the generation: the LLM writes a deterministic Python \verb|transform(grid) -> grid| function given a natural-language rule prompt. We execute this function on a suite of randomly-generated diverse grids to produce 4 consistent (input, output) demos per task. Because the function defines the rule, demos are guaranteed consistent by construction.

A secondary \textbf{overfit filter} accepts only tasks where a fresh base Volcan can memorize all 4 demos within a time budget (150 training steps, 75-second wall-clock cap). This rejects:
\begin{enumerate}
    \item Functions that crash or produce invalid outputs.
    \item Tasks too complex for our model class to learn even after memorization.
\end{enumerate}

Yield averages $\sim$75\% across our runs. Each accepted task is stored in native ARC JSON format plus the source Python code as metadata.

\subsection{Rule-seed library}

The LLM's rule is sampled from a library of seed descriptions. Our final library contains \textbf{83 rule seeds} spanning 12 categories: rotations, reflections, color substitutions, object extraction/filtering, object movement, containment/topology, counting, bounding boxes, neighbor operations, symmetry completion, tiling, and 2-step compositions. From this we generated two corpora:
\begin{itemize}
    \item \textbf{dream325}: 325 tasks, 20-seed library, 20 distinct rule families expressed.
    \item \textbf{dream\_wide}: 242 tasks, 83-seed library, 70 distinct rule families expressed.
\end{itemize}

\section{Training}

\subsection{Pretraining}

ICL pretraining: each training example is a 4-tuple ($K=3$ context demos, 1 query) sampled from a single task. The NCA runs Phase A on the 3 context demos (sequentially clamping color to their inputs/outputs) and the query input, then Phase B denoises the query target. Loss is masked denoising over the query's content cells (with padding downweighted).

Hyperparameters: batch size 4, AdamW lr $10^{-3}$ cosine-annealed to 0, 2000 gradient steps on dream\_wide, activity penalty $\lambda = 0.01$.

\subsection{Test-time training}

For each ARC-AGI-2 test task we load the pretrained base weights and:
\begin{enumerate}
    \item \textbf{$D_8$-augment} the task's demos (each demo is replicated $8\times$ under the dihedral group) to $8\times$ the training signal and enforce symmetry equivariance.
    \item \textbf{Freeze} all base parameters.
    \item \textbf{Attach LoRA rank-16 adapters} \citep{hu2022lora} to every $1 \times 1$ convolution in the update MLP.
    \item \textbf{Train only the adapters} for 150 steps with Adam at lr $10^{-2}$ ($5\times$ the pretraining lr, standard LoRA practice).
    \item \textbf{Inference}: run Phase A + Phase B on the test input, return the argmax color per cell.
\end{enumerate}

This recipe is essentially MIT TTT's transformer protocol (LoRA + $D_8$ augmentation) ported to an NCA substrate.

\section{Results}

\subsection{Main result}

Evaluated on 30 hand-picked ARC-AGI-2 training tasks (31 test inputs) with $(H, W)$ inputs and outputs of compatible shape:

\begin{center}
\begin{tabular}{lc}
\toprule
Configuration & TEST exact-match \\
\midrule
From-scratch + TTT (Week 2.5 baseline) & 0/10 \\
Pretrained + ICL (no TTT) & 0/31 \\
Pretrained + $D_8$-ensemble (no TTT) & 0/31 \\
Pretrained + TTT (no augmentation) & 1/31 (3.2\%) \\
\textbf{Pretrained + $D_8$-augmented TTT (LoRA rank-16)} & \textbf{3/31 (9.7\%)} \\
\bottomrule
\end{tabular}
\end{center}

The three passing tasks --- \texttt{25d8a9c8} (row-recolor), \texttt{b1948b0a}, \texttt{32597951} --- pass robustly under the full $D_8$ + LoRA TTT recipe.

\subsection{Ablation table}
\label{sec:ablation}

We ran \textbf{seven controlled experiments} after establishing the 9.7\% baseline, varying one axis at a time:

\begin{center}
\begin{tabular}{lccccc}
\toprule
Experiment & Params & Data & Seeds & TEST acc & $\Delta$ \\
\midrule
Baseline (dream43) & 111K & 43 & 17 & 3/31 & --- \\
+ data scale (dream325) & 111K & 325 & 20 & 3/31 & \textbf{0} \\
+ capacity (MoE) & 424K & 325 & 20 & 3/31 & \textbf{0} \\
+ rule diversity (dream\_wide) & 111K & 242 & 70 & 3/31 & \textbf{0} \\
+ LoRA rank 32 vs 16 & 111K & 242 & 70 & 2/5 probe & \textbf{neg.} \\
+ Hierarchical macro-cells & 255K & 242 & 70 & 3/31 & \textbf{0} \\
+ Hyper-TTT (meta-init LoRA) & 111K + 5.1M & 242 + 90 LoRA targets & 70 & \textbf{2/31} & \textbf{neg.} \\
\bottomrule
\end{tabular}
\end{center}

\textbf{No intervention moves the transfer accuracy upward}, even though data diversity clearly moves the pretraining peak (from 76\% to 80.7\%). Rank-32 LoRA is strictly worse than rank-16 on a 5-task probe, confirming that expressiveness of the TTT subspace is not the bottleneck. The hierarchical macro-cell experiment, directly ported from HRM's multi-timescale insight, does not move the transfer number either.

The Hyper-TTT experiment --- in which a 5.1M-parameter HyperNetwork was trained via supervised regression to predict ``good'' LoRA init weights from the 3 demos, using 90 supervised (demo $\rightarrow$ trained-LoRA) pairs from Stage-1 TTT runs --- \emph{regresses} to 2/31. The hypernet's val\_loss plateaued at 0.83 (vs.\ 1.0 zero-baseline) after 10 epochs before severe overfitting (train loss $\to 10^{-4}$ by epoch 200); the weakly-trained prior is an actively worse init than Kaiming zero-mean, losing the task \texttt{25d8a9c8} while keeping \texttt{b1948b0a} and \texttt{32597951}.

\textbf{Five of the seven passing configurations produce the same three passing tasks}, and the two regressions (rank-32, Hyper-TTT) both drop the easiest task (\texttt{25d8a9c8}).

\subsection{Efficiency comparison}

\begin{center}
\begin{tabular}{lcc}
\toprule
System & Params & ARC-AGI-2 \\
\midrule
Volcan (this work) & \textbf{111K} & \textbf{9.7\% ($n=31$)} \\
CompressARC \citep{liao2025compressarc} & 76K & 4.0\% \\
TRM \citep{jolicoeur2025trm} & 7M & 8.0\% \\
ARChitects \citep{architects2025} & $\sim$8B & 16.5\% \\
MindsAI 2025 & $\sim$8B & 12.6\% \\
NVARC \citep{sorokin2025nvarc} & $\sim$4B & 24.0\% \\
\bottomrule
\end{tabular}
\end{center}

At 1.6\% of TRM's parameter count we report a higher exact-match rate on our sample. We caution that our 30-task sample has a $\pm 5$ percentage-point confidence interval for 3/31; the true full-eval accuracy could be anywhere in the 5--15\% band. The efficiency claim relative to TRM is robust.

\section{Discussion: What Does and Does Not Move the Number}
\label{sec:discussion}

This paper's most informative finding may be its negative results. Across the project we ran 11 systematic experiments. We group them by what they tested:

\paragraph{Tests that uniformly produced 0/N.}
\begin{itemize}
    \item From-scratch NCA + TTT (Week 2.5, pre-data pipeline) --- confirms pretraining is necessary.
    \item Pretrained without TTT --- confirms TTT is necessary.
    \item $D_8$-ensemble inference without task-specific adaptation --- confirms any ``free'' inference-time trick needs signal to amplify.
\end{itemize}

\paragraph{Tests that produced 3/31.}
All six post-pipeline experiments that kept the $D_8$ + rank-16 LoRA TTT recipe constant (baseline, data-scale, MoE capacity, rule-seed diversity, hierarchical macro-cells, and the rank-16 LoRA init). The three passing tasks (\texttt{25d8a9c8}, \texttt{b1948b0a}, \texttt{32597951}) were identical across all six.

\paragraph{Tests that produced 2/31.}
\begin{itemize}
    \item Rank-32 LoRA --- more TTT capacity makes it worse, not better (loses \texttt{25d8a9c8}).
    \item Hyper-TTT (HyperNetwork-predicted LoRA init) --- a weakly-trained meta-init (val\_loss 0.83 after 10 epochs on 90 samples, then overfitting) is strictly worse than Kaiming zero-mean init (loses \texttt{25d8a9c8}).
\end{itemize}

\paragraph{Interpretation.} The stable 3/31 result across six independent axes of variation --- including hierarchical multi-timescale architecture --- strongly suggests that the ceiling is neither a capacity ceiling nor a data ceiling nor an information-bottleneck ceiling. The Hyper-TTT regression further suggests that the ceiling is also not an ``init-quality'' ceiling: the 3 passing tasks are solved because the 150-step TTT climb \emph{from zero} is enough, and the 27 failing tasks fail because no TTT climb from any reachable starting point --- random or hypernet-predicted --- converges on their rule within the budget. We propose two competing explanations:

\begin{enumerate}
    \item \textbf{Distribution-gap ceiling.} Despite 70 distinct rule seeds covering 12 categories, real ARC-AGI-2 rules draw from a much larger space of compositional transformations. The 27 failing tasks likely use rule structures for which our pretraining provides no useful prior, and 3-demo TTT is insufficient to bootstrap those rules from scratch on a 111K model.
    \item \textbf{Architecture-specific ceiling.} The NCA substrate with 4-connected local updates + mycelial + spectral may lack the representational primitives for certain ARC-AGI-2 rule families (e.g., rules requiring counting that depends on global context in ways local updates cannot compose). The three passing tasks may be the subset whose structure aligns with our substrate.
\end{enumerate}

We cannot distinguish these from our data. A follow-up experiment at $10\times$ model scale would cleanly separate them. We did not run this in the present work because our claim is explicitly the efficiency point, not the accuracy ceiling.

\section{Limitations and Future Work}

\paragraph{Sample size.} 30 tasks is a small sample. The 95\% confidence interval for 3/31 is roughly $(1.3\%, 19.4\%)$. A full ARC-AGI-2 evaluation would tighten this substantially.

\paragraph{Only one architectural family tested at scale.} We tested MoE (expert routing) as the capacity-scaling direction and found it did not help at our data scale. Dense scaling (e.g., \verb|mlp_hidden| $128 \to 1024$) was not run in this work.

\paragraph{Rule-seed library hand-curation.} Our 83 seeds were written by the authors. A more principled generation procedure (e.g., bootstrapped from ARC-AGI-1 solutions) could improve coverage.

\paragraph{LLM-as-logic quality.} We used qwen2.5:7b via Ollama. A larger LLM (gpt-oss:20b) was available locally but not used at the generation scale. NVARC's use of gpt-oss-120B for synthetic data \citep{sorokin2025nvarc} suggests this is an important axis.

\paragraph{Future work.} (a) Full ARC-AGI-2 evaluation to tighten the confidence interval. (b) Dense scaling ablation to separate distribution-gap vs.\ architecture ceilings. (c) Meta-learning (MAML-style) pretraining to make the base weights ``easy to adapt'' for TTT. (d) Curriculum data scaling with explicit difficulty tiers.

\section{Conclusion}

Volcan demonstrates that a 111K-parameter biologically motivated NCA can achieve 9.7\% on an ARC-AGI-2 subset --- exceeding TRM's 8\% on the full benchmark at 1.6\% of its parameter count. The five-pillar architecture, code-dreamed synthetic data pipeline, and $D_8$-augmented LoRA rank-16 TTT recipe form a reproducible small-model baseline for the task. Through seven controlled experiments --- including mixture-of-experts capacity scaling, hierarchical multi-timescale architecture, and a HyperNetwork-based meta-learned LoRA initialization --- we establish that this 9.7\% is a robust ceiling for our approach, with further accuracy gains requiring either a substantially different architectural substrate, a fundamentally richer synthetic distribution, or significantly more parameters than we targeted in this efficiency-focused study. The Hyper-TTT negative result is particularly informative: a warm-start init from a supervisedly-trained hypernetwork does not beat standard Kaiming init at this data regime, indicating the ceiling is not a function of adapter initialization quality.

\paragraph{Code and checkpoint availability.} All code, the dream\_wide task corpus, and the 9.7\% base checkpoint are available at \url{https://github.com/Tompark0927/volcan}.

\bibliographystyle{plainnat}
\bibliography{refs}

\appendix

\section{Project codebase}

Key modules in the Volcan repository:
\begin{itemize}
    \item \verb|src/volcan/volcan_cell.py| --- core NCA cell implementing all five pillars.
    \item \verb|src/volcan/moe.py| --- mixture-of-experts variant (negative-result ablation).
    \item \verb|src/volcan/hierarchy.py| --- macro-cell multi-timescale variant (negative-result ablation).
    \item \verb|src/volcan/hyperttt.py| --- HyperNetwork for meta-learned LoRA init (negative-result ablation).
    \item \verb|src/volcan/code_dreamer.py| --- LLM-to-Python-function pipeline.
    \item \verb|src/volcan/dream_filter.py| --- overfit-based consistency filter.
    \item \verb|src/volcan/lora.py| --- LoRA adapters for TTT, supports both dense and MoE updates.
    \item \verb|src/volcan/training_volcan.py| --- overfit + $D_8$ + LoRA TTT loop.
    \item \verb|src/volcan/pretrain.py| --- ICL pretraining.
    \item \verb|scripts/| --- runnable entrypoints for data generation, pretraining, evaluation, Hyper-TTT stages 1--2.
\end{itemize}

\section{The three passing tasks}

\begin{center}
\begin{tabular}{llp{7cm}}
\toprule
Task ID & Category & Notes \\
\midrule
\texttt{25d8a9c8} & Row-recolor & Small ($3 \times 3$) input, simple recoloring rule \\
\texttt{b1948b0a} & (uncategorized) & Passes robustly across all 5 main experiments \\
\texttt{32597951} & (uncategorized) & Passes robustly across all 5 main experiments \\
\bottomrule
\end{tabular}
\end{center}

These three are the consistent ``lucky'' subset --- tasks whose rule structure falls within the compositional reach of Volcan's five pillars + $D_8$ + LoRA TTT combination. The 27 failing tasks represent the concrete gap between our approach and general ARC-AGI-2 capability. Under both regression configurations (rank-32 LoRA, Hyper-TTT), the task that is lost is always \texttt{25d8a9c8} --- consistent with the interpretation that \texttt{25d8a9c8} is the ``easiest'' task and is the first to drop out when TTT capacity or init is perturbed unfavorably.

\end{document}
